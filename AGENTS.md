# Repository workflow

- Use plain `git` for commit and push requests in this repository.
- Do not require the GitHub CLI (`gh`) unless a task specifically needs GitHub features that plain Git cannot provide, such as opening or managing a pull request.
