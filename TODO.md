# Findings while testing

- Heading panel should be by default hidden.
- Save print output as PDF ... suggest name (same as file), as currently it is empty.
- Bottom text need to be fixed to relevant and with more instruction (shortcuts).

```html
<h1>MDPeek</h1>
<p>Press Ctrl+O or drag and drop a Markdown file here.</p>
<p>You can also pass its path on the command line:</p>
<p><code>mdpeek README.md</code></p>
```

- Upper text can be also i kind of **Help** and need to be also shown by presing F1 or clicking ? in the menu.
